{
  "name": "nookr.io",
  "version": "0.0.1",
  "description": "nookr.io",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node_modules/.bin/nodemon --watch /srv app.js",
    "renew": "rm -rf node_modules && npm install && node_modules/.bin/nodemon --watch /srv app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  },
  "devDependencies": {
    "nodemon": "^1.17.2"
  }
}
